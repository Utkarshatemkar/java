
public class ope {

    public static void main(String[] args) {
        // 1.aritmetic operation(perform basic mathematical ope: + ,-,%,*)
        // 2.Relational ope:(for comparision:Return boolean values)
        // ==(equal to);!=(not equal to);a>b(greaterthan);a<b(lessthan);>=(graterthanor
        // eual to);<=(lessthan or equalto)
        // 3.logical operator(used for boolen logic)
        // 1.&&(logical AND):returns true if both conditions are true
        // 2.||(logical OR):returns true if any one condition is true
        // 4,assignment operator(assign ope to value and asiign or update output to the
        // variable)+=;*=;%=;-=;/=
        // %=(this gives remainder of division)ex.10%3 remainder 1 output=1ex.15%4
        // remainder 3 output:3
        int a = 10, b = 20;
        a += 20;// 30=10+20
        System.out.println(a);
        a += 20;// 50=30+20
        System.out.println(a);
        a *= 2;// 100=50*2
        System.out.println(a);
        a /= 5;// 20=100/2
        System.out.println(a);
        a %= 3;// 2=20%3
        System.out.println(a);
        // 5.unary operator(operate on single operand)these operator used in loops
        // 1.+(unary plus)indicate a positive value
        // 2.-(unary minus)negative an expression
        // 3.increment:increase vlue by one:1.prefix ++a(increase before use)
        // 2.postfix a++(increase after use)
        // 4.--(decrement):decrease by one 1.prefix --a(decrease before use)
        // 2.postfix a--(decrease after use)

    }

}
